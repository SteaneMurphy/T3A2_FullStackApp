import React from "react";
import DisplayImage from "./DisplayImage";

const DestinationDescription = () => {
    return (
        <>
            <DisplayImage />
            <p>destination description, destination description, destination description, destination description, 
            destination description, destination description, destination description, destination description, 
            destination description, destination description, destination description, destination description, 
            destination description, destination description, destination description, destination description, 
            </p>
        </>
    )
};

export default DestinationDescription;